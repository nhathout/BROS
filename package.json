{
  "name": "bros",
  "private": true,
  "version": "0.1.0",
  "description": "BROS — Block ROS (Drag. Drop. Deploy.)",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/*/*"
  ],
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": "20.19.x",
    "pnpm": "10.x"
  },
  "engineStrict": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/nhathout/BROS.git"
  },
  "bugs": {
    "url": "https://github.com/nhathout/BROS/issues"
  },
  "homepage": "https://github.com/nhathout/BROS#readme",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter ./apps/desktop-app dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "format": "pnpm -r format",
    "typecheck": "pnpm -r typecheck",
    "ci": "pnpm install --frozen-lockfile && pnpm -r test",
    "bootstrap": "pnpm install -r",
    "preinstall": "node -e \"const semver=require('semver'); if(!semver.satisfies(process.version, '20.19.x')) { console.error('\\n Wrong Node version: ' + process.version + '. Please run \\'nvm use\\'.\\n'); process.exit(1); }\""
  },
  "keywords": [
    "ROS2",
    "Electron",
    "Blockly",
    "Gazebo",
    "Isaac",
    "robotics",
    "BROS"
  ],
  "author": "Noah Hathout nhathout@bu.edu>",
  "dependencies": {
    "electron": "^38.1.2",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.2",
    "vite": "^7.1.7",
    "semver": "^7.7.2",
    "rimraf": "^6.0.1"
  }
}
